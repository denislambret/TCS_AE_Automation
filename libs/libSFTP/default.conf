<?xml version="1.0"?>
<conf>
    <sftp_server>
        <!-- SFTP server name -->
        <computername>sftp.tcs.ch</computername> 
        <!-- SFTP user name -->
        <username>sftp_avance_pay_prod</username> 
        <!-- SFTP user password -->
        <userpwd></userpwd>
        <!-- SFTP private key path -->
        <privkey>D:\Scripts\Projects\SAP_AvancePay\sftp_avance_pay_prod.openssh.ppk</privkey>  
        <!-- SFTP default path -->
        <sftp_path>/advancePay/In</sftp_path>
        <!-- SFTP automatic key acceptance -->
        <sftp_acceptKey>true</sftp_acceptKey>
    </sftp_server>
</conf>